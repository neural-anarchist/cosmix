/** Head height as a fraction of H. Fixed in Phase 1; becomes a real
 * parameter alongside the tapered procedural torso in Phase 2. Shared
 * between the statue factory and the theory-section diagram so the two
 * never silently drift apart. */
export const HEAD_HEIGHT_RATIO = 0.12;
